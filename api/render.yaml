services:
  - type: web
    name: nl-bash-translator-api
    runtime: python
    rootDir: api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MODEL_NAME
        value: Qwen/Qwen2.5-Coder-0.5B-Instruct
